# RETIRED_COMPOSE_TOMBSTONE=1
#
# This legacy environment template is intentionally empty. Do not copy an old
# Buzz/Block production environment into this directory or into deploy/local.
# Follow deploy/local/README.md and initialize that stack with its pinned
# Carryforth Relay image contract instead.
