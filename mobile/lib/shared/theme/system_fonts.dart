/// Generic platform monospace family used for code and machine identifiers.
///
/// This intentionally delegates font selection to the operating system so the
/// mobile client does not need to distribute a separate monospace font asset.
const systemMonospaceFontFamily = 'monospace';
